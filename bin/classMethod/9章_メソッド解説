・解説
メソッドの基本構文

	修飾子 戻り値の型 メソッド名(引数) {
	    // 処理
	    return 戻り値;
	}

メソッドが戻り値を返さない場合は、「戻り値の型」に「void」を指定


●Human03クラス
String型の戻り値を返すgetProfileメソッドを定義する。
戻り値を返さない、String型の引数を1つとるgreetメソッドを定義する。
上記メソッド内でString型の引数としてnullが渡される可能性があるため
  nullチェックを行う(nullをそのまま扱うと、NullPointerExceptionを始め、
  様々なエラーのもととなるためチェックする癖をつける)。
上記以外はHuman01クラスと同様。
